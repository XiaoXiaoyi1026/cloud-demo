spring:
  application:
    name: userservice
  profiles:
    # 环境名称
    active: dev
  cloud:
    nacos:
      # nacos服务地址
      server-addr: localhost:81
      config:
        # 文件后缀名
        file-extension: yaml