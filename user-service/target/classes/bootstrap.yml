spring:
  application:
    # 服务名称
    name: userservice
  profiles:
    # 当前实例对应的配置文件环境名称，配置文件由服务名称-环境名称.后缀名组成
    active: dev
  cloud:
    nacos:
      # nacos服务地址, 用于在spring容器加载之前读取nacos上的配置信息
      server-addr: localhost:81
      config:
        # 配置文件后缀名
        file-extension: yaml